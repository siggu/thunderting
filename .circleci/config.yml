version: 2.1

executors:
  node-executor:
    docker:
      - image: cimg/node:20

jobs:
  install-dependencies:
    executor: node-executor
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package-lock.json" }}
      - run:
          name: Set up npm authentication (Netlify)
          command: echo "//registry.npmjs.org/:_authToken=${NETLIFY_AUTH_TOKEN}" > ~/.npmrc
      - run:
          name: Install Dependencies
          command: npm ci
      - save_cache:
          key: v1-dependencies-{{ checksum "package-lock.json" }}
          paths:
            - node_modules

  run-tests:
    executor: node-executor
    steps:
      - checkout
      - run:
          name: Run Tests with Coverage
          command: npm test -- --coverage
      - run:
          name: Upload Coverage to Codecov
          command: bash <(curl -s https://codecov.io/bash) -t $CODECOV_TOKEN

  deploy:
    executor: node-executor
    steps:
      - checkout
      - run:
          name: Build Project
          command: npm run build
      - run:
          name: Deploy to Netlify
          command: NETLIFY_AUTH_TOKEN=$NETLIFY_AUTH_TOKEN npm run deploy
      - run:
          name: Notify Sentry about Deployment
          command: |
            export SENTRY_AUTH_TOKEN=$SENTRY_AUTH_TOKEN
            npx @sentry/cli releases new "v${CIRCLE_SHA1}"
            npx @sentry/cli releases set-commits "v${CIRCLE_SHA1}" --auto
            npx @sentry/cli releases finalize "v${CIRCLE_SHA1}"

  lighthouse:
    executor: node-executor
    steps:
      - checkout
      - run:
          name: Run Lighthouse CI
          command: lhci autorun

workflows:
  version: 2
  build-test-deploy:
    jobs:
      - install-dependencies
      - run-tests:
          requires:
            - install-dependencies
      - deploy:
          requires:
            - run-tests
